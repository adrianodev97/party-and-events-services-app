import { Box, FormControl, styled, TextField, Typography } from '@mui/material'

export const MUIStyledNewsletterSection = styled('section')(({ theme }) => ({}))

export const MUIStyledNewsletterContent = styled(Box)(({ theme }) => ({}))

export const MUIStyledNewsletterTitle = styled(Typography)(({ theme }) => ({}))

export const MUIStyledNewsletterSubtitle = styled(Typography)(
  ({ theme }) => ({}),
)

export const MUIStyledNewsletterFormControl = styled(FormControl)(
  ({ theme }) => ({
    display: 'flex',
    flexDirection: 'row',
  }),
)

export const MUIStyledNewsletterInput = styled(TextField)(({ theme }) => ({}))
