import { styled } from '@mui/material'

export const MUIStyledMain = styled('main')(({ theme }) => ({
  width: '100%',
}))

export const MUIGlobalContainer = styled('div')(({ theme }) => ({
  maxWidth: '1232px',
  margin: '0 auto',
  padding: '0 1rem',
}))
